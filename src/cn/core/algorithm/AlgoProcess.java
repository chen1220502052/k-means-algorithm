package cn.core.algorithm;

import java.awt.image.BufferedImage;

public interface AlgoProcess {

	/**
	 * using an algorithm to process an image
	 * different ways 
	 * @param image
	 */
	public void process(BufferedImage image);
}
